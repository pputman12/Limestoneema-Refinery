<ul>
  <% if Admin::HazardsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_hazards_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_hazard_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::HazardsController.sortable? and Hazard.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Hazards"),
                 admin_hazards_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Hazards"),
                 admin_hazards_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>